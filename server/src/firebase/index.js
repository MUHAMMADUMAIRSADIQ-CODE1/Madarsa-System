const {
  initializeFirebase,
  getFirebaseApp,
  getAuth,
  getFirestore,
  getStorage,
} = require('../config/firebase');

module.exports = {
  initializeFirebase,
  getFirebaseApp,
  getAuth,
  getFirestore,
  getStorage,
};
